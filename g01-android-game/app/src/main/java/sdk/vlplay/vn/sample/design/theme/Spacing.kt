package sdk.vlplay.vn.sample.design.theme

import androidx.compose.ui.unit.dp

/**
 * 8dp linear spacing scale — parity iOS [VLSpacing].
 */
object VLSpacing {
    val xs = 4.dp
    val sm = 8.dp
    val md = 16.dp
    val lg = 24.dp
    val xl = 32.dp
    val safeMargin = 20.dp
}

/**
 * Corner radius scale — parity iOS [VLRadius].
 */
object VLRadius {
    val sm = 4.dp
    val md = 8.dp     // default
    val lg = 16.dp
    val pill = 999.dp
}

/**
 * Elevation tokens — parity iOS [VLElevation].
 */
object VLElevation {
    val ambientRadius = 12.dp
    val ambientYOffset = 4.dp
}
